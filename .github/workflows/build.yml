name: Build

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Core tests
        run: ./Scripts/test-core.sh
      - name: Build without signing
        run: ./Scripts/build.sh
